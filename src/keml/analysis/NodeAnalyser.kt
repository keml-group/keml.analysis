package keml.analysis

import keml.Conversation
import org.apache.commons.csv.CSVFormat;
import java.io.File

fun createCSV(conv: Conversation, path: String) {
		//File(path).bufferedWriter().use { out ->
 
		//CSVFormat.DEFAULT.print(out).apply {}
	
		//}

		}

class NodeAnalyser {
	
	companion object {
		fun createCSV(conv: Conversation, path: String) {
		//File(path).bufferedWriter().use { out ->
 
		//CSVFormat.DEFAULT.print(out).apply {}
	
		//}

		}
	}

}